---
name: A machine for the catalogue
about: Add your computer, so somebody has a crowd to hide in
labels: persona
---

<!--
Run the probe in an ORDINARY browser — Chrome, not Fury — then:

    cargo run -p fury-detect -- persona baselines/<capture>.json > persona.json

Read the file before attaching it. A persona describes YOUR computer: its GPU,
screen, fonts, audio latency and installed speech voices. That is what makes it
useful and it is also what makes it yours.
-->

**The machine**

- OS and version:
- GPU:
- Screen:

**Roughly how common is it?**

A guess is fine. It becomes the `weight`, which decides how often the machine is
offered — a persona nobody else owns is conspicuous on its own, so rare machines
are deliberately offered less.

**persona.json**

<!-- attach it, or paste it in a fenced block -->
