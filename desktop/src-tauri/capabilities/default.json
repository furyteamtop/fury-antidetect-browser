{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window access for the main window. Nothing else: every server call goes through this app's own #[tauri::command]s, which are not plugin commands and so need no permission here, and no plugin that reaches the filesystem, the shell or the network from JavaScript is installed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
