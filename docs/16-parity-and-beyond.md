# 16 — Паритет и то, что дальше

Разбор четырёх проектов ([08](08-competitors.md)) дал одну закономерность и один
неприятный вывод про порядок работ. Оба стоят того, чтобы записать их до списка
задач, потому что они этот список меняют.

## Закономерность: открытый лончер, закрытое ядро, команд нет

| Проект | Лончер | Ядро | Команды |
|---|---|---|---|
| ShardBrowser | MIT, 593★ | закрытый бинарник с их CDN | нет |
| Donut Browser | AGPL, 3557★ | Wayfern, закрытый | нет |
| rustcloak | MIT, 2★ | CloakBrowser, закрытый | нет |
| CloakBrowser | MIT-обёртка, 29.6k★ | **свой, но проприетарный** | нет |

Четыре проекта подряд, ни одного исключения. Ниша Fury — открытое ядро плюс
командный слой — подтверждена в четвёртый раз, и это не самоуспокоение: это
значит, что копировать их функции безопасно, потому что ни одна из них не
занимает нашу позицию.

Второе следствие менее приятное. Их лончеры **лучше нашего**, и заметно. Donut —
1572 коммита зрелого менеджера профилей. ShardBrowser — 170 профилей устройств,
MCP, SDK для трёх языков. У нас 27 персон и примеры на Python.

## Порядок: сначала релиз, потом функции

У всех четырёх есть то, чего нет у нас: **работающая загрузка**. У Donut ночные
сборки, у ShardBrowser бинарники под три платформы, CloakBrowser ставится через
`pip install`.

У нас ноль тегов, нет подписи, нет ядра под Windows.

Добавлять двадцать функций к тому, что нельзя скачать, — неверный порядок. Пока
релиза нет, любая новая функция увеличивает только количество непроверенного
кода. Поэтому фаза 0 этого плана — не функция.

## Пробелы, сведённые вместе

Отмечено, у кого взято. «есть» значит найдено в нашем коде, а не в намерениях.

### Лончер

| | у нас | взято у |
|---|---|---|
| Локальный HTTP API | есть (`agent/src/http.rs`, порт 35000) | — |
| Импорт/экспорт cookies | есть (`profile.cookies.*`) | — |
| Экспорт/импорт проектов | есть (`projects.export`) | — |
| Ротация прокси | есть (`proxies.rotate`) | — |
| Теги и поиск | есть (`store.rs`, колонка `tags`) | — |
| **Расширения браузера** | **нет вообще** | rustcloak, Donut, ShardBrowser |
| **MCP-сервер** | нет | ShardBrowser, Donut |
| **SDK пакетами** | только примеры | ShardBrowser |
| **Автозагрузка ядра** | нет, намеренно | rustcloak, ShardBrowser |
| **Импорт профилей из Chrome/Edge/Brave** | нет | Donut |
| **WireGuard на профиль** | нет | Donut |
| **DNS-блокировка на профиль** | нет | Donut |
| **Роль браузера по умолчанию** | нет | Donut |
| **Размер профиля и очистка кэша** | нет | rustcloak |
| **Массовый импорт прокси** | нет | ShardBrowser |
| **Локализация интерфейса** | нет | Donut |

### Ядро

| | у нас | взято у |
|---|---|---|
| Персон в каталоге | 27 | ShardBrowser: 170 |
| **TLS ClientHello (JA3/JA4)** | закомментирован в `series` с измерением | ShardBrowser |
| **p0f / отпечаток TCP** | нет | ShardBrowser |
| **QUIC/HTTP-3 через UDP-релей** | признан не-пробелом ([08](08-competitors.md)) | ShardBrowser |
| **WebAuthn** | нет | ShardBrowser |
| **Заголовки валидации Google** | нет | ShardBrowser |
| **Поведенческая гуманизация** | нет | CloakBrowser (`humanize=True`) |

Про QUIC и JA3 — записи в `series` уже содержат измерения, по которым их
отложили. Прежде чем брать их обратно, надо перечитать те измерения, а не список
чужих функций: у ShardBrowser QUIC работает через **их** UDP-релей, а у нас
релей TCP, и это другое решение, а не отсутствующее.

## Чего не умеет никто

Здесь лежит разница между «догнали» и «лучше всех». Все четыре пункта — прямое
следствие открытого ядра, то есть скопировать их закрытые проекты не могут.

**1. Воспроизводимая сборка ядра.** Самый сильный ответ на вопрос из issue #105
CloakBrowser — «я не вижу, что изменено в исходниках Chromium», на который
мейнтейнер не ответил. Опубликованных патчей мало: нужно, чтобы третья сторона
могла собрать из них бинарник и получить тот же хеш. Никто из четверых этого не
может по определению.

**2. Измерение на каждый релиз, воспроизводимое кем угодно.** У ShardBrowser
доказательство — скриншоты с imgur. У CloakBrowser — таблица в README. У нас
есть `tools/detect-suite` и записанные капчуры; не хватает того, чтобы каждый
релиз нёс подписанный отчёт, который любой может перепроверить на своём
бинарнике. Мы единственные, у кого стенд уже есть.

**3. Аудит в команде.** `audit_events` существует и переживает удаление
организации ([migration 0007](../server/migrations/0007_audit_outlives_the_org.sql)).
Ни у кого из четверых нет многопользовательского режима вообще, значит нет и
вопроса «кто и когда открывал этот профиль». Для агентства это и есть причина
покупки.

**4. Персона как контракт, а не сид.** У rustcloak отпечаток — одно `u64`. У
ShardBrowser — профиль устройства из каталога. Ни у кого нет проверки, что
комбинация возможна: macOS с RTX 4060 не бывает. У нас есть тест
`the_sample_persona_is_self_consistent`; нужно довести его до правила, которое
отказывает при сохранении невозможной персоны, а не только в тестах.

## План

Пять фаз. У каждой задачи — где она живёт, критерий готовности и откуда взята:
**[П]** паритет (есть у конкурентов, нет у нас), **[Н]** нет ни у кого.

Оценки в человеко-неделях одного разработчика. Суммарно 24–33 недели, но фазы
1-2 и 3 идут параллельно: фаза 3 упирается в машины и сертификаты, фаза 1 — нет.

---

### Фаза 0 — сделать скачиваемым · 4-5 недель · блокирует всё

Ни одна задача ниже не имеет смысла, пока продукт нельзя установить. У всех
четырёх конкурентов загрузка есть; у нас ноль тегов.

| # | Задача | Где | Готово, когда | Оценка |
|---|---|---|---|---|
| 0.1 | Ядро под Windows | `core/args/windows-x64.gn` | `tools/verify-windows.ps1` проходит целиком, без пропусков | 1 нед + машина |
| 0.2 | Подпись и нотаризация macOS | `tools/release/sign-core.sh` | скачанный `.tar.xz` открывается на чужом Mac без обхода Gatekeeper | 3 дня + сертификат |
| 0.3 | Первый релиз | `.github/workflows/`, `tools/release/package.sh` | тег `v0.1.0`, артефакты под 3 цели, `SHA256SUMS`, страница Releases не пустая | 1 нед |
| 0.4 | Verify для остальных 22 патчей | `core/verify/verify-NNNN.py` | 27 из 27 патчей имеют скрипт; `self-hosted.yml` реально отработал хоть раз | 2-3 нед |

**0.4 важнее, чем выглядит.** Сейчас пять скриптов на двадцать семь патчей, и
workflow, который их гоняет, не запускался ни разу — у него была ошибка,
делавшая запуск невозможным. Это единственное, что отделяет «патчи написаны» от
«патчи работают».

---

### Фаза 1 — паритет по лончеру · 6-8 недель

По убыванию отношения пользы к труду.

| # | Задача | Где | Готово, когда | Оценка | |
|---|---|---|---|---|---|
| 1.1 | **Расширения** | новый `agent/src/ext.rs`, `launcher.rs` | CRX3 ставится, ID стабилен после клонирования, набор по умолчанию на проект | 2 нед | [П] |
| 1.2 | Импорт профилей из Chrome/Edge/Brave | `agent/src/import.rs` | профиль с cookies и историей переезжает и открывается | 1 нед | [П] |
| 1.3 | Размер профиля и очистка кэша | `ipc.rs`: `profiles.usage`, `profiles.trim` | размер виден в списке; очистка сносит кэш и **не** трогает cookies и историю | 3 дня | [П] |
| 1.4 | Массовый импорт прокси | `ipc.rs`: `proxies.import` | вставка 500 строк `user:pass@host:port` создаёт 500 прокси | 2 дня | [П] |
| 1.5 | MCP-сервер | новый `tools/mcp/` | Claude Desktop видит профили и умеет запускать | 1 нед | [П] |
| 1.6 | SDK пакетами | `sdk/python`, `sdk/node` | `pip install fury-browser` и `npm i fury-browser` работают | 1 нед | [П] |
| 1.7 | Локализация интерфейса | `desktop/src/i18n/` | русский и английский переключаются | 1 нед | [П] |

**1.1 — самый крупный пробел из всех.** Расширений нет ни в каком виде: ни
установки, ни CRX, ни флага в лончере. Есть у всех троих. Разбор CRX3 у
rustcloak (`crates/core/src/ext.rs`) под MIT — читать и переиспользовать можно
законно.

---

### Фаза 2 — паритет по сети · 4-6 недель

| # | Задача | Где | Готово, когда | Оценка | |
|---|---|---|---|---|---|
| 2.1 | WireGuard на профиль | `agent/src/relay.rs` | профиль ходит через WG-конфиг, при падении туннеля трафик не идёт напрямую | 2 нед | [П] |
| 2.2 | DNS-блокировка на профиль | `agent/src/relay.rs` | список блокировки применяется, счётчик виден | 1 нед | [П] |
| 2.3 | JA3/JA4 | новый патч `0200-tls-clienthello` | `browserleaks.com/tls` даёт JA4 настоящего Chrome 150 | 2-3 нед | [П] |

**2.3 — сначала перечитать `series`.** Диапазон 0200-0299 пуст не по забывчивости:
там записаны измерения, по которым его отложили. У ShardBrowser QUIC работает
через **их** UDP-релей, у нас релей TCP — это другое решение, а не отсутствующее.
Менять надо записанную причину, а не добавлять функцию рядом с ней.

---

### Фаза 3 — то, чего нет ни у кого · 6-10 недель

Здесь появляется преимущество, а не паритет. Все четыре пункта — следствие
открытого ядра, скопировать их закрытые проекты не могут.

| # | Задача | Где | Готово, когда | Оценка | |
|---|---|---|---|---|---|
| 3.1 | **Воспроизводимая сборка** | `core/build/`, `docs/03` | посторонний собирает из опубликованных патчей и получает **тот же хеш** | 3-4 нед | [Н] |
| 3.2 | **Отчёт измерений на релиз** | `tools/detect-suite/`, `package.sh` | рядом с `SHA256SUMS` лежит подписанный отчёт, который любой перепроверяет на своём бинарнике | 1-2 нед | [Н] |
| 3.3 | **Персона-контракт** | `shared-rs/src/persona.rs` | сохранить macOS с RTX 4060 нельзя — отказ с объяснением, а не только тест | 1 нед | [Н] |
| 3.4 | Каталог до 170+ персон | `shared-rs/src/catalogue.rs` | 170 машин, каждая от измеренной базы | 2-3 нед | [П] |
| 3.5 | Поведенческая гуманизация | новый патч + `detect-suite` | **сначала измерение**: ловит ли что-нибудь текущую неотличимость | 3-4 нед | [Н] |

**3.1 — самый сильный из всех.** Прямой ответ на вопрос из issue #105
CloakBrowser: «я не вижу, что изменено в исходниках Chromium». Мейнтейнер не
ответил, и не мог: бинарник проприетарный. Опубликованных патчей мало — нужно,
чтобы третья сторона собрала из них тот же самый бинарник.

**3.5 — самый рискованный.** Прежде чем писать, надо измерить, ловит ли
что-нибудь текущую неотличимость. `tools/detect-suite/cdp-timing.py` — образец
того, как такой вопрос ставится. Если не ловит, пункт закрывается измерением,
как закрылся QUIC.

---

### Фаза 4 — командный слой, который и есть ниша · 4-6 недель

Ни у одного из четырёх нет многопользовательского режима вообще. Всё, что здесь,
у нас уже наполовину есть и не доведено до продукта.

| # | Задача | Где | Готово, когда | Оценка | |
|---|---|---|---|---|---|
| 4.1 | Аудит в интерфейсе | `desktop/src/`, `server/src/api.rs` | владелец видит, кто и когда открывал профиль | 1 нед | [Н] |
| 4.2 | Приглашения и роли в UI | `desktop/src/` | человека зовут по ссылке, роль выдаётся мышью | 1-2 нед | [Н] |
| 4.3 | Общие прокси на организацию | `server/`, `agent/src/store.rs` | прокси покупается раз и виден команде | 1 нед | [Н] |
| 4.4 | Передача профиля между людьми | `bundle.rs`, `server/` | профиль уходит другому вместе с логинами, без пересылки файлов | 1-2 нед | [Н] |

---

### Что делать первым, если время только на одно

**0.1-0.3.** Продукт, который нельзя скачать, не проигрывает конкурентам по
функциям — он в них не участвует.

## Чего в плане намеренно нет

**Автозагрузка ядра одним кликом.** `install_core.rs:15-18` объясняет, почему
принимает путь, а не URL: скачать и выполнить — это канал в антидетект-браузер с
адреса, который не является прокси профиля. Ровно то же возражение записано про
автообновления в [README](../README.md). ShardBrowser и rustcloak это делают; мы
осознанно не делаем, и если менять решение — то менять записанную причину, а не
добавлять функцию рядом с ней.

**Группы профилей.** Строились и были убраны как более слабый дубль тегов. Donut
их имеет; это не повод возвращать.

**Роль браузера по умолчанию.** У Donut это осмысленно, потому что он замена
повседневного браузера. Fury запускает профили для работы, и профиль, открывшийся
по случайной ссылке из почты, — это не удобство, а испорченная сессия.
