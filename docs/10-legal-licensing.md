# 10 — Лицензии, право, ограничения

## Chromium

**Лицензия:** BSD-3-Clause на код Google. Зависимости в `third_party/` — смесь
(MIT, Apache-2.0, LGPL, MPL и др.), все совместимы с открытой публикацией.

Форкать и распространять можно. Так делают Brave, Vivaldi, Opera, Edge, Bromite, ungoogled-chromium.

### Что нельзя

| Нельзя | Почему |
|---|---|
| Название «Chrome», «Chromium», «Google» в продукте | Товарные знаки, отдельно от лицензии на код |
| Логотипы Google/Chrome, включая иконку | То же |
| Google API keys официальной сборки | Условия использования. Sync, Safe Browsing, Geolocation, Translate — свои или отключить |
| Widevine CDM | Проприетарный бинарь, распространяется по отдельной лицензии Google |

**Заявлять Chrome в User-Agent можно.** Это не использование товарного знака в продукте,
а декларация совместимости. Так делают все форки.

### Widevine — практическая проблема

Без Widevine не работают Netflix, Spotify Web, Amazon Prime и часть сайтов с DRM-контентом.
Хуже для антидетекта: `navigator.requestMediaKeySystemAccess('com.widevine.alpha', ...)`
у настоящего Chrome возвращает успех с определённым `robustness`, а у вас — отказ.
**Это готовый детект в три строки JS.**

Варианты:

1. **Запросить лицензию у Google на распространение Widevine CDM.** Бесплатно для
   браузерных проектов, но требует заявки и одобрения. Brave и Vivaldi так и сделали.
   Для проекта, позиционируемого как антидетект, шансы одобрения невысоки — стоит
   позиционировать заявку как privacy-браузер.
2. **Патчить ответ `requestMediaKeySystemAccess`,** чтобы он врал об успехе. Это закрывает
   детект по факту наличия, но ломается, если сайт реально попытается воспроизвести
   защищённый контент. Для большинства антифрод-проверок достаточно.
3. **Оставить как есть** и принять детект. Не рекомендуется.

Реалистичный путь: (2) в v1, попытка (1) параллельно.

### Проверено 31.07.2026 — диагноз точный

`enable_widevine = true` собирается без ошибок, но этого **недостаточно**:

```
com.widevine.alpha      NotSupportedError
com.microsoft.playready NotSupportedError
org.w3.clearkey         available
```

ClearKey работает — значит EME в сборке функционирует целиком, и дело
именно в **регистрации** Widevine, а не в поломке шифрованных медиа.

CDM при этом на машине **есть**: Chrome 150.0.7871.187 держит его в
`/Applications/Google Chrome.app/.../Libraries/WidevineCdm`, ровно для нашей
версии. Копирование каталога в `Chromium Framework.framework/.../Libraries/`
ситуацию не меняет — сборка ищет CDM не там или ждёт регистрации через
component updater.

### Решено 31.07.2026 — вариант (1) не понадобился

```
com.widevine.alpha       available
org.w3.clearkey          available
com.microsoft.playready  NotSupportedError   (как и у Chrome на macOS)
```

Работает связка из трёх вещей:

1. `enable_widevine = true` — включает поддержку;
2. `bundle_widevine_cdm = true` — по умолчанию ложен, потому что upstream
   привязывает его к `is_chrome_branded`, а без него сборка ждёт CDM от
   component updater, который мы намеренно отключаем (эта загрузка ушла бы
   мимо прокси профиля, с реального IP);
3. [`core/build/link-widevine.sh`](../core/build/link-widevine.sh) — кладёт CDM
   из установленного на машине Chrome в `third_party/widevine/cdm/mac/<arch>/`,
   сверяя версию с пином.

Сборка сама раскладывает CDM в бандл, вручную копировать не нужно.

**Причина, по которой это заняло три захода, к Widevine отношения не имела.**
Скрипт быстрой сборки вызывал `autoninja` напрямую, минуя `build.sh`, а
`args.gn` копирует именно `build.sh`. Изменения GN-аргументов молча
игнорировались часами: `gn args --list` показывал `enable_widevine = false`
при `true` в исходном файле. Диагностику дополнительно зашумляло предупреждение
про `enable_nacl`, удалённый из M150. Оба исправлены: скрипт синхронизирует
аргументы перед каждой сборкой, устаревший арг убран.

Урок общий: **если конфигурация не действует, сначала проверь, что она вообще
доехала**, а не ищи причину в самой функции.

Отдельно по праву: **использовать CDM, уже установленный на машине пользователя,
и распространять его в своём инсталляторе — разные вещи.** Первое приемлемо,
второе требует лицензии Google.

Сделано 02.08.2026: [`agent/src/widevine.rs`](../agent/src/widevine.rs) при
старте агента один раз копирует CDM из установленного на машине Google Chrome в
тот же макет, который дал бы `bundle_widevine_cdm`. С машины ничего не уходит и
ничего не распространяется — блоб уже лицензирован на неё.

Первая версия провалилась так, как это выглядит успехом. `manifest.json` лежит
двумя каталогами выше библиотеки (`<WidevineCdm>/_platform_specific/<platform>/`),
а копирование поднималось на один: библиотека вставала на место, лог печатал
«staged the Widevine CDM» с числом байт, браузер отвечал `NotSupportedError` —
Chromium читает из манифеста версию интерфейса CDM и без него библиотеку не
регистрирует. Нашлось вопросом к браузеру, а не чтением лога. Теперь
отсутствие манифеста после копирования — громкая ошибка, и на это есть тест.

Измерено сквозь: собранный CDM удалён, агент запущен, профиль поднят через
хельсинкский выход — `available: com.widevine.alpha`, релизный гейт 13/13 при
`crossContext.disagreements = 0`.

Не фатально по построению: машина без Chrome получает браузер без CDM. Это
детектируемо и работает; отказ стартовать разменял бы проблему отпечатка на
простой.

То же с **проприетарными кодеками** (H.264, AAC): включаются флагами
`proprietary_codecs = true` + `ffmpeg_branding = "Chrome"`. Юридически это патентный
вопрос (MPEG-LA), а не лицензия Google. Для открытого проекта, распространяющего
бинарные сборки, формально нужна патентная лицензия на H.264. На практике десятки
OSS-браузеров собираются с этими флагами. Решение — риск, который принимается
осознанно; полностью его снять нельзя, потому что без H.264 сборка детектируется мгновенно.

## Лицензия собственного кода

Решено и написано 02.08.2026 (`6edcc35`, `dcca576`) — ниже не рекомендация, а
то, что лежит в репозитории:

| Компонент | Лицензия | Где записано |
|---|---|---|
| `core/patches/` | BSD-3-Clause | [`core/patches/LICENSE`](../core/patches/LICENSE). Контекстные строки патчей — код Google, они остаются BSD независимо от чьего-либо решения. Строки, которые добавляет Fury, включая созданные серией с нуля файлы `components/fury/`, — BSD **по выбору, а не по обязанности**: BSD ничего не навязывает окружающему коду, они могли быть AGPL. Так собранный из серии Chromium несёт один набор условий, и копилефт не достаёт до того, кому нужен был браузер |
| `agent/`, `server/`, `desktop/`, `shared-rs/`, `tools/`, `core/build/`, `core/args/`, `core/verify/`, `deploy/`, `docs/` | **AGPL-3.0-or-later** | [`LICENSE`](../LICENSE), [`NOTICE`](../NOTICE) §1. Не даёт превратить сервер в закрытый SaaS без раскрытия правок. Замерено 02.08.2026: SPDX-заголовок стоит в 83 файлах вне `core/src` |
| `shared/` схемы | Apache-2.0 | [`NOTICE`](../NOTICE) §3. Чтобы совместимость реализовывал кто угодно. SPDX-заголовков там нет: все три файла — JSON, а в JSON нет комментариев |

Одна ловушка в именах: `shared/` — схемы под Apache-2.0, `shared-rs/` — крейт
под AGPL. Похожи, условия разные.

Логика: вы делаете бесплатный продукт. AGPL защищает от сценария «конкурент берёт ваш
код, добавляет биллинг, продаёт закрытый SaaS и не отдаёт исправления». Именно этот
сценарий очень вероятен на этом рынке.

**Альтернатива, если приоритет — распространение:** Apache-2.0 везде. Больше
контрибьюторов, больше внедрений, но и коммерческие форки без отдачи.

**Смешивать нельзя как попало:** BSD → AGPL совместимо (BSD-код можно включать в
AGPL-проект). Обратно — нет. Поэтому патчи держим отдельным каталогом с явным
`LICENSE` внутри.

## Двойное назначение и acceptable use

Продукт двойного назначения: то же самое, что позволяет вести десять легальных
кабинетов маркетплейса или проверять рекламу конкурентов, позволяет и злоупотребления.

Для открытого проекта это решается не техническими ограничениями (их обойдут
форком за час), а рамками участия:

- `ACCEPTABLE_USE.md` в корне: для чего проект, для чего нет.
- В issue tracker **не принимаются** запросы вида «обойти проверку личности на X»,
  «сделать бот для массовой регистрации на Y». Это правило комьюнити, и оно
  определяет, кто вокруг проекта соберётся.
- Не поставляем готовых сценариев под конкретные площадки. Local API — общий
  инструмент, а не «бот для Instagram».
- Не даём никаких обещаний обхода конкретных антифрод-систем в маркетинговых
  материалах. Публикуем только измеримые технические результаты ([07](07-detection-baseline.md)).

Это не юридическая защита, а гигиена: она определяет, будет ли проект восприниматься
как privacy-инструмент или как фрод-тулкит. От этого зависит очень практичная вещь —
не забанят ли вам репозиторий, аккаунт разработчика Apple и сертификат подписи.

## Что упрощается из-за бесплатности

По сравнению с платным SaaS снимаются целые категории проблем:

- **Нет эквайринга.** Это была бы главная головная боль: классические PSP такие
  проекты не берут, остаются рискованные провайдеры и крипта.
- **Нет KYC/AML клиентов** — вы никому не оказываете услугу.
- **Нет обязательств по SLA и хранению чужих данных** — self-hosted, данные у пользователя.
- **Нет GDPR-обязанностей контроллера** — вы не обрабатываете персональные данные
  пользователей продукта. Только если поднимете свой сервер сбора телеметрии — тогда
  обязанности появляются, поэтому телеметрия должна быть opt-in и анонимной.

Остаётся:
- ответственность за распространяемые бинарники (подпись, отсутствие малвари);
- патентный вопрос по H.264;
- репутационный риск.

## Подпись кода

| Платформа | Что нужно | Стоимость (порядок) |
|---|---|---|
| macOS | Apple Developer Program, Developer ID Application, нотаризация | $99/год |
| Windows | **EV Code Signing** сертификат (на токене или в облаке HSM) | $300-600/год |

Windows OV-сертификат не даёт мгновенной репутации в SmartScreen — новые сборки будут
блокироваться неделями. EV даёт репутацию сразу. Для проекта, который выпускает релиз
раз в месяц, это критично: с OV каждая новая версия будет проходить период недоверия.

**Windows EV** оформляется на юрлицо, и вот здесь для открытого проекта
действительно нужна юридическая оболочка — иначе подписывать нечем.

**Для macOS это неверно, и раньше здесь было написано так, будто верно.**
Apple Developer Program регистрируется и на физическое лицо: юрлицо, D-U-N-S и
сайт на своём домене нужны только для варианта Organization. Цена та же $99.
Разница не в возможности подписать, а в том, **чьё имя навсегда попадает в
подпись**: при Individual в бандле стоит `Developer ID Application: Имя Фамилия
(TEAMID)`, и это читается одной командой с любой скачанной копии. Разбор
развилки и всё остальное про сертификат — [17](17-apple-signing.md).

То есть 0.2 не заблокирована отсутствием юрлица. Она заблокирована сертификатом.

## macOS: entitlements для Chromium

Хелпер-процессы Chromium требуют исключений hardened runtime, иначе не запустятся
после нотаризации:

```xml
<key>com.apple.security.cs.allow-jit</key><true/>
<key>com.apple.security.cs.allow-unsigned-executable-memory</key><true/>
<key>com.apple.security.cs.disable-library-validation</key><true/>
```

Раздельные entitlements для основного бандла и для `*.app/Contents/Frameworks/*Helper*.app` —
типовая точка провала нотаризации при первой сборке.
