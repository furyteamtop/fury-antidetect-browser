# Copy to .env and fill in. Never commit .env.

POSTGRES_PASSWORD=change-me
MINIO_ROOT_USER=antik
MINIO_ROOT_PASSWORD=change-me-too

# Base URL agents will use to download bundles from MinIO. Must be reachable
# from operator machines, not just from inside the compose network.
S3_PUBLIC_ENDPOINT=http://localhost:9000
